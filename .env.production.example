# Copy to .env.production (gitignored) before running `npm run build` or `npm run deploy`.
# Vite injects these at build time — they MUST be set before building.

VITE_API_URL=https://evisa-api.onrender.com
VITE_PAYPAL_CLIENT_ID=
